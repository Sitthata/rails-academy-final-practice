<%= form_with(model: @quest, local: true, class: "bg-base-300 rounded-lg p-4 w-full") do |f| %>
  <div class="flex flex-col space-y-5">
    <%= f.text_field :name, placeholder: "Quest Name", class: "input w-full" %>
    <%= f.label :completed do %>
      <%= f.check_box :completed, class: "checkbox" %>
      mark as completed
    <% end %>
    <%= f.submit "Add Quest", class: "btn btn-primary" %>
  </div>
<% end %>